If you don't run the edt-config.sh script in ../scripts, the
serial-port comms with the Adimec camera will not work.

EosAdimecEdtMain.x will bomb out with serial-port errors if
the camera is not initialized with that script.
